# Paloma runtime dependencies.
#
# Preferred install (editable + tests):
#   pip install -e ".[dev]"
#
# This file mirrors [project] dependencies in pyproject.toml for plain
# ``pip install -r requirements.txt`` workflows.

numpy>=1.21
scipy>=1.7
astropy>=5.0
scikit-image>=0.19
opencv-python>=4.5
matplotlib>=3.4
photutils>=1.5
lightkurve>=2.4
python-dotenv>=1.0
FITS_tools @ git+https://github.com/keflavich/FITS_tools.git
